Round: D106

Commits:
- 2f4c4df47784a9455c5f1e246912d156df280705 D106-1: rewrite the failing D105 claude cache-invalidation test to a scenario the resolver can actually produce

Summary:
- Replaced the impossible Claude cache-invalidation expectation with a reachable scenario: when the cached fixed-name Claude executable disappears, `locateClaudeCli` can refresh to another fixed-name candidate available on PATH.
- Confirmed `resolveOnPath()` does not scan PATH directories or discover arbitrary renamed binaries such as `claude-new.exe`; it only checks its configured candidate names in order.

Claude CLI load-bearing assessment:
- The Codex CLI stale-cache guard remains load-bearing because Codex has a versioned `%LOCALAPPDATA%\OpenAI\Codex\bin\<hash>\codex.exe` fallback and a real deleted-folder failure mode.
- The Claude Code CLI side is less clearly load-bearing for updater behavior: if Claude overwrites the same `claude.exe` path in place, the cached path remains valid; if an updater emits an arbitrary renamed binary, this resolver cannot discover it. The existence check is still useful and harmless for deletion/fallback cases where another fixed-name candidate is present.

Verification:
- `npm test -- tests/claude-code-cli.test.ts`: passed, 1 test file, 27 tests.
- `npm run typecheck`: passed.
- `npm test`: passed, 35 passed | 1 skipped (36) test files; 347 passed | 1 skipped (348) tests.
- `git diff --check`: passed.

