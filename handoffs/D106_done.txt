D106 done

Round: D106
Status: npx tsc --noEmit clean (exit 0); npm test green (349 passed, 1 skipped live-smoke, 0 failed). D105's previous "347/347 passed" claim was a real bug - the claude test I added in D105 encoded an impossible scenario (resolveOnPath doesn't scan for arbitrary renamed files) and failed deterministically. D106 fixes the test to a scenario the resolver CAN produce. The Claude side of D105's self-healing-cache fix is preserved (with the documented caveat that it doesn't correspond to a known live failure mode for Claude's in-place overwrite, unlike Codex's versioned-subfolder churn).

Test changes:
- D105's "cache invalidates when the cached path disappears" test rewrote from "fake `claude-new.exe` exists, expect second call to return it" (impossible - resolveOnPath only ever matches fixed literal names) to "PATH contains two dirs (C:/bin and D:/bin); first call primes on the bundled-binary name in C:/bin; deleted = true makes that old path no longer exist; second call resolves to the same bundled-binary name in D:/bin (a different fixed-name candidate) - which the resolver CAN return". The fake `existsFake` is now a clean two-line predicate: `if (filePath === oldPath) return !deleted; if (filePath === newPath) return deleted;` - the resolver's inner loop checks `node_modules/.../claude.exe`, `claude.exe`, `claude`, `claude.cmd` against each PATH dir in order; with oldPath (C:/bin/...claude.exe) deleted and newPath (D:/bin/claude.exe) present, the second call's resolver returns newPath after the C:/bin miss. D106 makes the test reproduce a real scenario rather than a structurally-impossible one.
- The other D105 claude test (cache short-circuits on common case) was correct already - I verified the same existsFake works for it (the trace showed the first call returns C:\bin\node_modules\@anthropic-ai\claude-code\bin\claude.exe; existsCallCount increments by 1 on the second call because the existence check is exactly what D105 adds; no PATH walk or directory rescan on the cache-hit path).

Source unchanged: the D105-1 source fix to claude-code-cli/locate.ts is preserved as-is. I considered whether the Claude fix is "load-bearing" given how that CLI's updater behaves (hypothesis: in-place overwrite means the cached path still passes exists() so the self-healing check would never trigger for a real update in that tool). The handoff asked me to say so plainly in the completion report rather than leave an untested/mis-tested change in place. The fix is NOT removed: in-place overwrite is the most common case, but a Claude CLI self-updater that quarantines the old binary under a different name (or any future fix that moves the bundled binary under node_modules/.../bin/) WOULD cause the stale path to exist-check fail and trigger re-resolution. The cost of the existence check is one stats() call per lookup - negligible against the cost of an entire leader call - and the architectural property (don't trust a cached path blindly, verify on hit) is sound as a defensive default. I'm leaving the change in.

Acceptance: npx tsc --noEmit clean, npm test green (349 passed, 1 skipped, 0 failed - the same 1 skipped live-smoke test that's been skipped throughout this codebase's run). Note: the previous D105 claim of "347/347 passed" was wrong because the test the claim was based on was the one I had to rewrite in D106. This D106 claim is verified by an actual `npm test` re-run before writing this marker.

No live model call. The D105/D106 changes are pure filesystem-resolution logic in two functions; tests are deterministic via injected `exists`/`readdir`/`stat` fakes.
</content>
</invoke>