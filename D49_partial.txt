D49 partial

Round: D49
Status: code complete, live verification blocked by Anthropic API quota.

Implementation commits (pending):
- D49-1 (pending): restore Claude live harness executable isolation in scripts/live-d47-claude.ts.
  - The dirty pre-existing edit was REVERTED (with one retained improvement: `claude --version` log line so the harness records which binary it is exercising).
  - Re-introduced `locateClaudeCli({ env: process.env })` (the same path Tandem uses at runtime) so the Windows .cmd shim can never get in the way of the harness.
  - Re-introduced `mkdtemp(path.join(os.tmpdir(), "tandem-d47-"))` for the worker scenario; the prior dirty edit's fixed `C:/tmp-d47-test` path was thrown out.
  - Re-introduced RAW STDOUT print for the leader direct-question scenario; worker RAW STDOUT print was already present and was kept.
  - Wired `SCHEMAS` map to the schema kinds jsonSchemaFor() supports, removed direct `string` schema names.
- D49-2: no new unit test added. Existing test "prefers Anthropic's Windows executable over the cmd shim" (tests/claude-code-cli.test.ts:101) already covers the rule; per D49 instructions redundant tests are not required.

Static verification:
- npm run typecheck: green.
- npm test: 175 passed, 1 skipped (live-smoke), 0 failed.
- git diff --check: clean.

Live verification (npx tsx scripts/live-d47-claude.ts):
- Harness recorded binary: C:\Users\huizh\AppData\Roaming\npm\node_modules\@anthropic-ai\claude-code\bin\claude.exe (NOT claude.cmd - confirmed the production rule).
- claude --version: "2.1.144 (Claude Code)".
- Leader scenario raw stdout (excerpt):
  {"type":"result","subtype":"success","is_error":true,"api_error_status":429,...,"result":"You've hit your limit · resets 5pm (America/New_York)",...}
- Worker scenario raw stdout (excerpt):
  {"type":"result","subtype":"success","is_error":true,"api_error_status":429,...,"result":"You've hit your limit · resets 5pm (America/New_York)",...}
- structured_output: undefined for both (because of the 429, not a harness bug).
- The harness correctly used locateClaudeCli's preferred Anthropic binary and a fresh mkdtemp cwd for the worker scenario. The 429 is an external quota/auth blocker, not a harness defect.

D49_done.txt deliberately NOT created: per HANDOFF_GPT5_D49 acceptance bar, "do not create D49_done.txt unless the round genuinely meets acceptance or the handoff is explicitly amended." Live acceptance did not meet because Anthropic quota blocked both scenarios with HTTP 429.
</content>
</invoke>