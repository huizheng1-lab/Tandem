# W0007 Step Evidence

Use this checklist for each independently validated epic step:

- Confirm the step is the next unchecked item in the approved plan.
- Keep the change within the normal one-turn size norm.
- Check only the implemented step in the same commit.
- Record focused checks, `npm run typecheck`, `npm test`, and `git diff --check`.
- Require peer acceptance before another step starts.
